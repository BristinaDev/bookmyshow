<% if user_signed_in? %>
  <nav class="navbar navbar-main navbar-expand-lg position-sticky top-0 navbar-dark bg-light py-2 py-lg-1 px-0 border-bottom">
		<div class="container-fluid">
			<a class="navbar-brand d-flex align-items-center gap-2">
				<span class="text-dark h5 font-bold ls-dark">
					<% if user_signed_in? %>
					<%= current_user&.email %></p>
					<% end %>
				</span>
			</a>
			<button class="navbar-toggler me-n2 ms-auto text-start" type="button" data-bs-toggle="modal" data-bs-target="#mobile_nav" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			<div class="navbar-nav order-lg-5 ms-auto d-none d-lg-inline-flex align-items-center">
				<li class="nav-item">
					<%= link_to 'Logout', destroy_user_session_path, class: 'nav-link text-dark' %>
				</li>
			</div>
		</div>
  </nav>
<% end %>